{
  "annotations": { "list": [] },
  "editable": true,
  "graphTooltip": 1,
  "id": null,
  "liveNow": true,
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "title": "Stage duration",
      "type": "timeseries",
      "targets": [{ "expr": "histogram_quantile(0.95, sum(rate(ltx_duration_seconds_bucket[5m])) by (le, stage))", "legendFormat": "{{stage}}", "refId": "A" }]
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "ops" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "title": "Runs by pipeline",
      "type": "timeseries",
      "targets": [{ "expr": "sum(increase(ltx_runs_total[5m])) by (engine, offload)", "legendFormat": "{{engine}} {{offload}}", "refId": "A" }]
    },
    {
      "datasource": { "type": "loki", "uid": "loki" },
      "gridPos": { "h": 10, "w": 24, "x": 0, "y": 8 },
      "id": 3,
      "title": "Worker + stack logs",
      "type": "logs",
      "targets": [{ "expr": "{job=~\"ltx-worker|ltx-docker\"}", "refId": "A" }]
    }
  ],
  "refresh": "5s",
  "schemaVersion": 39,
  "tags": ["ltx", "inference"],
  "templating": { "list": [] },
  "time": { "from": "now-30m", "to": "now" },
  "title": "LTX inference",
  "uid": "ltx-inference",
  "version": 1
}
